# .github/workflows/ci_full.yaml
name: CI - Loader Complete Test Suite

on:
  push:
    paths:
      - 'services/loader_testing/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'services/loader_testing/**'
  workflow_dispatch:

jobs:
  # ============================================================
  # LINT JOB - Code Quality Checks
  # ============================================================
  lint:
    name: Lint (Black + Flake8)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/loader_testing
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          pip install uv
          uv sync --extra dev
      
      - name: Run Black formatting check
        run: |
          uv run black --check src/api-service/api
      
      - name: Run Flake8 linting
        run: |
          uv run flake8 src/api-service/api

  # ============================================================
  # UNIT TESTS - Fast, No External Dependencies
  # ============================================================
  unit-tests:
    name: Unit Tests (Coverage ≥50%)
    runs-on: ubuntu-latest
    needs: lint
    defaults:
      run:
        working-directory: services/loader_testing
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          pip install uv
          uv sync --extra dev
      
      - name: Run unit tests with coverage
        env:
          PYTHONPATH: ${{ github.workspace }}/services/loader_testing/src/api-service
        run: |
          uv run python -m pytest tests/unit/ -v --cov=api --cov-fail-under=50 --cov-report=term --cov-report=html
      
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-coverage
          path: services/loader_testing/htmlcov/

  # ============================================================
  # INTEGRATION TESTS - With Database, Mocked AI
  # ============================================================
  integration-tests:
    name: Integration Tests (DB + Mocked AI)
    runs-on: ubuntu-latest
    needs: lint
    
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_pass
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    defaults:
      run:
        working-directory: services/loader_testing
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client
      
      - name: Initialize test database
        env:
          PGPASSWORD: test_pass
        run: |
          psql -h localhost -U test_user -d test_db -f tests/setup/init_test_db.sql
      
      - name: Install dependencies
        run: |
          pip install uv
          uv sync --extra dev
      
      - name: Run integration tests
        env:
          PYTHONPATH: ${{ github.workspace }}/services/loader_testing/src/api-service
          DATABASE_URL: postgresql://test_user:test_pass@localhost:5432/test_db
          GOOGLE_CLOUD_PROJECT: test-project-ci
          ARTICLES_TABLE_NAME: articles
          VECTOR_TABLE_NAME: chunks_vector
        run: |
          uv run python -m pytest tests/integration/ -v --no-cov

  # ============================================================
  # SYSTEM TESTS - Full Stack with Docker Compose, Mocked AI
  # ============================================================
  system-tests:
    name: System Tests (Full Stack + Mocked AI)
    runs-on: ubuntu-latest
    needs: [unit-tests, integration-tests]
    
    defaults:
      run:
        working-directory: services/loader_testing
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          pip install uv
          uv sync --extra dev
      
      - name: Run system tests with Docker Compose
        env:
          USE_MOCKED_AI: "true"
          GOOGLE_CLOUD_PROJECT: test-project-ci
        run: |
          docker compose -f docker-compose.test-ci.yml up --build --abort-on-container-exit
      
      - name: Show container logs on failure
        if: failure()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Container Logs (on failure):"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          docker compose -f docker-compose.test-ci.yml logs
      
      - name: Cleanup Docker resources
        if: always()
        run: |
          docker compose -f docker-compose.test-ci.yml down -v

  # ============================================================
  # TEST SUMMARY - Aggregate Results
  # ============================================================
  test-summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [lint, unit-tests, integration-tests, system-tests]
    if: always()
    
    steps:
      - name: Display test results summary
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "                  TEST SUITE SUMMARY"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "  📋 Lint:             ${{ needs.lint.result }}"
          echo "  🧪 Unit Tests:       ${{ needs.unit-tests.result }}"
          echo "  🔗 Integration:      ${{ needs.integration-tests.result }}"
          echo "  🚀 System Tests:     ${{ needs.system-tests.result }}"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      
      - name: Check for failures
        run: |
          if [[ "${{ needs.lint.result }}" == "failure" ]] || \
             [[ "${{ needs.unit-tests.result }}" == "failure" ]] || \
             [[ "${{ needs.integration-tests.result }}" == "failure" ]] || \
             [[ "${{ needs.system-tests.result }}" == "failure" ]]; then
            echo ""
            echo "❌ TEST SUITE FAILED - See job details above"
            echo ""
            exit 1
          fi
          
          echo ""
          echo "✅ ALL TESTS PASSED!"
          echo ""
